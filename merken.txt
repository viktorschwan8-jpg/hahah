## Stand: 27.08.2026 - Quest Bot Status

### Was FUNKTIONIERT:
1. **Token-Login via Chrome Extension** - Funktioniert! Chrome startet, Extension setzt Token, Discord zeigt /app (eingeloggt)
2. **REST API Quests laden** - Funktioniert! /api/v9/quests/@me gibt 21-29 Quests pro Account zurueck
3. **REST API Video-Quests** - Funktioniert! heartbeat/post-fortheart schicken Fortschritt an Discord
4. **REST API Rewards claimen** - Funktioniert! /api/v9/quests/{id}/claim-reward
5. **rpc_railway.js** - Laeuft auf Railway mit Quest Worker (nur Video-Quests via REST API)
6. **Alle Tokens korrekt** - 22 Tokens in tokens.txt, alle funktional

### Was NICHT funktioniert:
1. **Orion Script in Chrome injizieren** - Script wird injiziert (injected Status), aber:
   - Orion laeuft nicht durch (kein orionLock, kein UI Element)
   - Keine Fehler sichtbar (keine console errors)
   - Vermutlich: Orion braucht Discord Desktop/Webpack Module die im Browser nicht so funktionieren
   - Oder: Orion bricht leise ab weil Webpack Module nicht geladen sind

2. **GAME Quests via REST API** - çerçne moeglich:
   - Discord prueft ob ein Desktop-Client (RunningGameStore) das Spiel gestartet hat
   - REST API allein reicht nicht - braucht Browser/Desktop Spoofing

3. **Chrome Quest Runner (chrome_quest.js)** - Token-Login klappt, aber Orion inseted nicht richtig

### Was noch zu tun waere:
1. **Orion Webpack Debug** - Pruefen ob `window.webpackChunkdiscord_app` im Chrome Browser existiert
2. **Orion Fehler finden** - Orion bricht wahrscheinlich ab weil Stores nicht gefunden werden
3. **Desktop Client Alternative** - Fuer GAME Quests braucht man den Desktop Client (Ndiscord/Equicord)
4. **Oder: Anderes Script** - Ein REST-only Quest Bot der VIDEO+GAMEQuests ohne Browser spoofed

### Token-Login Extension:
- Eigene Extension in `discord-token-login/`
- Manifest V3, Content Script auf discord.com/login
- Speichert Token in chrome.storage, setzt localStorage, laedt Seite neu
- Funktioniert mit --load-extension Flag in Chrome

### Dateien:
- `rpc_railway.js` - Hauptscript fuer Railway (REST API Quests + RPC)
- `chrome_quest.js` - Chrome CDP Runner (Token-Login + Orion Injection)
- `test_quick.js` - Debug Test fuer Orion
- `discord-token-login/` - Chrome Extension fuer Token-Login
- `freedekoscript.txt` - Orion Script (Referenz)
- `tokens.txt` - 22 Discord Tokens
